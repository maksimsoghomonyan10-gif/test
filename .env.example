# OpenAI API Configuration
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
#   - gpt-3.5-turbo (fast and cheap)
#   - gpt-4 (most powerful, requires paid account)
#   - gpt-4-turbo (balanced, requires paid account)
# See MODEL_GUIDE.md for details
LLM_MODEL=gpt-4o-mini

# Browser Configuration
# true = headless mode (no browser window)
# false = visible browser window
HEADLESS=false

# Maximum steps the agent can take before stopping
MAX_STEPS=50

# Development Configuration
DEBUG=false
VERBOSE=false
